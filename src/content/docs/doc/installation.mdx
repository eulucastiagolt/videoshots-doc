---
title: Installation
description: How to install VideoShorts in your project.
---

## Via NPM

```bash
npm install videoshorts
```

## Via Yarn

```bash
yarn add videoshorts
```

## Via PNPM

```bash
pnpm add videoshorts
```

## CDN (jsDelivr)

### JavaScript

```html
<script src="https://cdn.jsdelivr.net/gh/eulucastiagolt/videoshorts@1.6.7/dist/videoshorts.min.js"></script>
```

### CSS

```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/eulucastiagolt/videoshorts@1.6.7/dist/videoshorts.min.css">
```

## CDN (unpkg)

### JavaScript

```html
<script src="https://unpkg.com/videoshorts/dist/videoshorts.min.js"></script>
```

### CSS

```html
<link rel="stylesheet" href="https://unpkg.com/videoshorts/dist/videoshorts.min.css">
```

## Manual Download

You can also download the files directly from the GitHub repository:

1. Visit [github.com/eulucastiagolt/videoshorts](https://github.com/eulucastiagolt/videoshorts)
2. Download the `dist/videoshorts.min.js` and `dist/videoshorts.min.css` files
3. Include them in your project

```html
<link rel="stylesheet" href="path/to/videoshorts.min.css">
<script src="path/to/videoshorts.min.js"></script>
```

## Verifying Installation

After installation, verify that the library is available:

### ES Modules

```js
import VideoShorts from 'videoshorts';
import 'videoshorts/dist/videoshorts.min.css';

console.log(VideoShorts.version);
```

### CDN / Script Tag

```js
console.log(VideoShorts.version);
```

If everything is correct, you will see the current version in the console.
